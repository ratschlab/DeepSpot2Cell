# PyTorch and Lightning — core training stack
# torch/torchvision/torchaudio are installed by conda (see environment.yaml);
# keep them here only for pip-only installs (docker, venv, etc.)
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0
lightning>=2.2.0
torchmetrics>=1.3.0

# Scientific stack
numpy>=1.23.0
pandas>=1.5.0
scipy>=1.11.0
scikit-learn>=1.2.0
numba>=0.56.0
llvmlite>=0.39.0

# Spatial / single-cell biology
anndata>=0.8.0
scanpy>=1.9.0
squidpy>=1.2.0
hest>=0.2.0

# Morphology model backends
timm>=0.9.12
transformers>=4.42.0
huggingface_hub>=0.23.0
einops>=0.7.0

# Image I/O and processing
opencv-python>=4.9.0
pillow>=10.0.0
pyvips>=2.2.1
shapely>=2.0
h5py>=3.8.0

# Plotting
matplotlib>=3.8.0
plotnine>=0.12.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
psutil>=5.9.0
